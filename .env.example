# Copie para `.env` e ajuste.
# Servidor local para desenvolvimento:
VITE_ONLINE_URL=ws://localhost:8787
# Servidor hospedado (produção) — use sempre wss:// :
# VITE_ONLINE_URL=wss://seu-servidor.onrender.com
